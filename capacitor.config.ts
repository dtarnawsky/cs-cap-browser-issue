import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.cap.browser.issue',
  appName: 'cs-cap-browser-issue',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
